# Optional: URL of the cloud-save API (see /server). If unset, the game runs
# entirely on browser localStorage, exactly as before — this is what the
# Vercel demo uses.
# VITE_API_URL=https://your-server.example.com
