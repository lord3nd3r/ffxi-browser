PORT=8787
# Comma-free single origin or * for any. Set this to your game's URL in production,
# e.g. https://ffxi-browser.vercel.app
CORS_ORIGIN=*
# Generate with: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=changeme
# Optional: override the SQLite file location
# DB_PATH=./data/vanadiel.db
